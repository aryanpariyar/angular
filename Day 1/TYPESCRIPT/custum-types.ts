        // => Custom Types in TypeScript:
        let circlaPoint = (x,y) => {
                // ...
        } 
        circlaPoint(9,8);
        // IN this function we have 2 parameter what if here is more than 2 parametrs this is 
        // to hard for us so instead of passing variaable we need to object 

        let circlePoint= (Point) => {
            // ...
        }

        circlePoint({
            
        })